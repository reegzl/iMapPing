# iMapPing
iMapPing is a high-performance, multi-threaded Python utility designed to validate massive lists of IMAP server routing configurations. By executing rapid, non-invasive TCP socket checks and SSL handshakes concurrently, it safely separates operational mail infrastructure from dead targets without risking account lockouts or IP blacklisting.
